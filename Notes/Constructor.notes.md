# A constructor is a special method and have a complulsion to have a name of
# it's class name. And it doesn't need to return anything, so there is no type
# is defined to that constructor.
# When you are creating an object it saves into the stack memory of the JVM,
# and it's object data stores into the heep valuse and it's address store to
# the stack.
# When we define constructor so before anything while creasting the object that
# constructor calls, it executes first. then further data or methods initialize