# when we keep data and methods into the same instance, class it called as Encapsulation.
It is simillary like we kept many things in a box.
And then there is private and public keywords, who can directly get things out of the box.