super() and this()

super calls the constructor of the parent, super class, and this calls the current class constructor only. Only that class in which it's been written

Here we got a concept of method overridding also, because first an object checks into it's current state only then it goes to parent if 